# Индекс ссылок

Навигация по хранилищу теперь распределена по переходным страницам `Главная.md` в каждой папке.

- Откройте [[Главная]] для входа в корневую навигацию.
- Далее переходите по разделам через локальные `Главная.md` внутри нужных директорий.
