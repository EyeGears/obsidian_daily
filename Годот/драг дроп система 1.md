---
tags:
  - area/годот
---

Система драг энд дропа через колижены 

var pmp = Vector2()
var isdragg = false
# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass


func _on_input_event(viewport, event, shape_idx):
	if event.is_action_pressed("ui_touch"):
		print(event)
		#get_tree().set_input_as_handled()
		pmp = event.position
		isdragg = true
	pass # Replace with function body.

func  _input(event):
	if not isdragg:
		return
	if event.is_action_released("ui_touch"):
		pmp = Vector2()
		isdragg = false
	
	if isdragg and event is InputEventMouseMotion:
		position += event.position - pmp
		pmp = event.position
		
	pass