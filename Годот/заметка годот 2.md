tween.tween_property(self, "visible_ratio", 6.9, 2.0).from(0.0)

// плавно меняет значения у объекта, заполнение

modulate.a8 = 1
// делает спрайт невидимым

![[image-8.png]]

создавал разрушаемость как то по этой структуре но не помню как, там постоянно были проблемы с коллизиями и ваще не так считало

> [!note] Код для таймера
> var min = 1
> var sec = 60
> var zero = ""
> Called when the node enters the scene tree for the first time.
> func _ready():
> 	min = 1
> 	sec = 60
> 	zero = ""
> 	pass 
> 
> 
 Called every frame. 'delta' is the elapsed time since the previous frame.
> func _process(delta):
> 	if sec == 0:
> 		min -=1
> 		sec = 59
> 		zero = ""
> 	pass
> 
> 
> func _on_timer_timeout():
> 	sec -= 1
> 	var format_string = "Score: 0%s:%s%s"
> 	text = format_string % [min, zero,sec]
> 	if sec < 11:
> 		zero = 0
> 	if sec > 10:
> 		zero = ""
> 	pass 


$kurs.global_position = get_global_mouse_position() // курсор собственный

![[image-10.png]]

// создание зеркальной текстуры

Для ноды paralax2d в параметрах scroll offset для смены фона
scroll_offset.x = speed * delta // скорость позволяет создавать для разных слоев фона разную скорость и создать эффект паралакса.

в параметре scale указывается степень скорости для каждого слоя отдельно


animationSprite2D.flip_h = true \\ позволяет изменить положение спрайта с право на лево

queue.free() // уничтожение объекта

get_tree().change_scene_to_file("сцена на которую игрок переходит при вызове этой функции")

команда instantiate создает копию команды
add_child(добавить переменную на сцену)

var x = randi_range(50,159) // позволяет установить случайный диапозон чисел диапазон  


![[image-16.png]]

